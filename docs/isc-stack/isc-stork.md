# Install ISC STORK on Ubuntu 22.04

### Update repository

```bash
sudo apt update && sudo apt upgrade
```

### Then install PostgreSQL database

```bash
sudo apt install postgresql
```

Switch to postgres

```bash
su postgres
```
